import ccxt
import os
from dotenv import load_dotenv
import time
load_dotenv()
api_key = os.getenv("5huWOQsBrwBCFPTL6YBPxzpV0pCZHx5nd9qDm9MB5CHngY2OCglQmlDlsqBBE0VV")
api_secret = os.getenv("0uaiZXnDF5S1k17kz3DPRla4XSisTVYHZ7qvuQ9Bb4WVwsDhNdbv2Pad6xzXAuc0")
symbol = "XRP/EUR"
min_budget_eur = 100  # Mindestbudget
buy_ratio = 0.9       # 90 % von EUR verwenden
# Binance-Setup
exchange = ccxt.binance({
   'apiKey': api_key,
   'secret': api_secret,
   'enableRateLimit': True,
})
exchange.options['defaultType'] = 'spot'
def get_balance(asset):
   balance = exchange.fetch_balance()
   return balance[asset]['free']
def get_price():
   ticker = exchange.fetch_ticker(symbol)
   return ticker['ask']
def buy_xrp():
   eur_balance = get_balance('EUR')
   print(f"EUR verfügbar: {eur_balance:.2f} EUR")
   if eur_balance < min_budget_eur:
       print("⚠️ Nicht genug EUR. Kein Kauf.")
       return
   price = get_price()
   amount_to_spend = eur_balance * buy_ratio
   xrp_amount = round(amount_to_spend / price, 1)  # XRP erlaubt 1 Dezimalstelle
   print(f"✅ Kaufe {xrp_amount} XRP bei {price:.4f} EUR/XRP")
   order = exchange.create_market_buy_order(symbol, xrp_amount)
   print("🟢 Kauf abgeschlossen:", order)
def sell_xrp():
   xrp_balance = get_balance('XRP')
   if xrp_balance < 1:
       print("⚠️ Zu wenig XRP zum Verkaufen.")
       return
   xrp_to_sell = round(xrp_balance, 1)
   print(f"✅ Verkaufe {xrp_to_sell} XRP")
   order = exchange.create_market_sell_order(symbol, xrp_to_sell)
   print("🔴 Verkauf abgeschlossen:", order)
# -------------------------------
while True:
   print(f"\n⏱ {datetime.datetime.now()} – Starte neuen Prüf-Durchlauf...")
   try:
       # ENTRY-Signal prüfen
       df = fetch_ohlcv()
       signal, message = check_entry_signal(df)
       print("🔍", message)
       if signal:
           print("✅ ENTRY erkannt – starte Kauf!")
           buy_xrp()
       else:
           print("⏸ Kein Einstieg")
       # EXIT-Signal z. B. bei XRP > 1
       sell_xrp()
   except Exception as e:
       print("❌ Fehler im Durchlauf:", str(e))
   print("⏳ Warte 10 Minuten...")
   time.sleep(600)  # 10 Minuten
